{
  email info@asiatravelplan.com
}

(security_headers) {
  header {
    Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    X-Content-Type-Options "nosniff"
    X-Frame-Options "SAMEORIGIN"
    Referrer-Policy "strict-origin-when-cross-origin"
  }
}

(noindex_headers) {
  header {
    X-Robots-Tag "noindex, nofollow, noarchive, nosnippet, noimageindex"
  }
}

(staging_html_no_cache_headers) {
  @staging_html path / /index.html /404.html /bookings.html /booking.html /persons.html /marketing_tour.html /marketing_tours.html /standard-travel-plans.html /settings.html /emergency.html /privacy.html /booking-confirmation.html /traveler-details.html
  header @staging_html {
    Cache-Control "no-store, no-cache, must-revalidate"
    Pragma "no-cache"
    Expires "0"
  }
}

(staging_static_cache_headers) {
  @staging_static path /assets/* /frontend/scripts/* /frontend/data/* /shared/* /site.webmanifest
  header @staging_static Cache-Control "public, max-age=300, stale-while-revalidate=600, must-revalidate"
}

asiatravelplan.com {
  import security_headers
  encode zstd gzip
  root * /placeholder

  @legacy_backend_page path /backend.html
  handle @legacy_backend_page {
    redir * /bookings.html 302
  }

  @legacy_marketing_tours_page path /tours.html
  handle @legacy_marketing_tours_page {
    redir * /marketing_tours.html 302
  }

  @legacy_marketing_tour_page path /tour.html
  handle @legacy_marketing_tour_page {
    redir * /marketing_tour.html 302
  }

  @booking_confirmation_page path /booking-confirmation.html
  handle @booking_confirmation_page {
    rewrite * /frontend/pages/booking-confirmation.html
    file_server
  }

  @traveler_details_page path /traveler-details.html
  handle @traveler_details_page {
    rewrite * /frontend/pages/traveler-details.html
    file_server
  }

  @manifest path /site.webmanifest
  header @manifest Content-Type "application/manifest+json"

  file_server
}

www.asiatravelplan.com {
  import security_headers
  redir https://asiatravelplan.com{uri} permanent
}

staging.asiatravelplan.com {
  import security_headers
  import noindex_headers
  import staging_html_no_cache_headers
  import staging_static_cache_headers
  encode zstd gzip
  root * /srv

  handle_errors {
    @page_not_found expression {err.status_code} == 404
    rewrite @page_not_found /frontend/pages/404.html
    file_server
  }

  @site_root path /
  handle @site_root {
    rewrite * /frontend/pages/index.html
    file_server
  }

  @legacy_backend_page path /backend.html
  handle @legacy_backend_page {
    redir * /bookings.html 302
  }

  @legacy_marketing_tours_page path /tours.html
  handle @legacy_marketing_tours_page {
    redir * /marketing_tours.html 302
  }

  @legacy_marketing_tour_page path /tour.html
  handle @legacy_marketing_tour_page {
    redir * /marketing_tour.html 302
  }

  @persons_page path /persons.html
  handle @persons_page {
    rewrite * /frontend/pages/persons.html
    file_server
  }

  @html_pages path /index.html /404.html /bookings.html /booking.html /persons.html /marketing_tour.html /marketing_tours.html /standard-travel-plans.html /settings.html /emergency.html /privacy.html /booking-confirmation.html /traveler-details.html
  handle @html_pages {
    rewrite * /frontend/pages{path}
    file_server
  }

  @staging_access path /staging-access/*
  handle @staging_access {
    reverse_proxy backend:8787
  }

  @protected not path /staging-access/* /api/whatsapp/webhook /integrations/meta/webhook /site.webmanifest
  forward_auth @protected backend:8787 {
    uri /staging-access/check
  }

  @robots path /robots.txt
  rewrite @robots /robots-staging.txt

  @backend path /auth/* /api/* /public/v1/* /integrations/*
  handle @backend {
    reverse_proxy backend:8787
  }

  @manifest path /site.webmanifest
  header @manifest Content-Type "application/manifest+json"

  handle {
    @existing_site_file file {
      try_files {path} {path}/index.html
    }
    handle @existing_site_file {
      file_server {
        hide /srv/.git /srv/.gitignore /srv/backend /srv/deploy /srv/documentation /srv/scripts /srv/docker-compose*.yml /srv/.env* /srv/README* /srv/AGENTS.md
      }
    }

    error 404
  }
}

api-staging.asiatravelplan.com {
  import security_headers
  import noindex_headers
  encode zstd gzip
  reverse_proxy backend:8787
}

auth-staging.asiatravelplan.com {
  import security_headers
  import noindex_headers
  encode zstd gzip
  reverse_proxy keycloak:8080
}
