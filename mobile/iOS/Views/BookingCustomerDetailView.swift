import SwiftUI

struct BookingCustomerDetailView: View {
    var body: some View {
        Text("Customer records are removed in this branch. Use the booking detail instead.")
            .foregroundStyle(.secondary)
            .navigationTitle("Customer")
    }
}
