import SwiftUI

struct TravelGroupsListView: View {
    var body: some View {
        Text("Travel groups are removed in this branch.")
            .foregroundStyle(.secondary)
            .navigationTitle("Travel Groups")
    }
}
