In booking.html when I select the customer language, there should be a selector “edit in “XX” and “edit in English and EN->XX”
If translation is selected  there should be a translation table at the bottom of the page with all terms used on the page in the same segments as the booking page. Each segment has a dirty status and a translate button.

local translation